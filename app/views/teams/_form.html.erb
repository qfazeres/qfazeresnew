<%= form_for(@team) do |f| %>
<% if @team.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@team.errors.count, "error") %>
    prohibited this project from being saved:</h2>

  <ul>
    <% @team.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <div class="col-md-6">
    <%= f.label :user_id, "Usuário" %><br>
    <%= select("team", "user_id", User.all.collect {|u| [ u.email, u.id ] }, {include_blank: 'Selecione'}, class: "form-control") %>
  </div>
</div>
<div class="field">
  <div class="col-md-6">
    <%= f.label :project_id, "Projeto" %><br>
    <%= select("team", "project_id", Project.all.collect {|p| [ p.title, p.id ] }, {include_blank: 'Selecione'}, class: "form-control") %>
  </div>
</div>

<div class="clearfix"></div>

<br>
<div class="col-md-12">
  <div class="actions pull-right">
    <%= f.submit "Salvar Equipe", class: "btn btn-warning" %>
  </div>
</div>
<% end %>
